import intAspectRatio from "./aspect_ratio";

export interface intOptions {
    apply: string,
    // styleLocale: string,
    aspectRatio: intAspectRatio,
    colorInactive: string,
    colorActive: string,
    compatibility: boolean,
    autoplay: boolean,
    muted: boolean,
}