// ==================================================
export default interface intControls {
    playing: boolean,
    fullscreen: boolean,
    pictureInPicture: boolean,
    volume: boolean,
    rangerVolume: number,
    rangerProguess: number,
    duration: string,
    currentTime: number,
}