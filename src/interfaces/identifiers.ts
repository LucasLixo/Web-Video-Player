// ==================================================
export default interface intIdentifiers {
    all: string,
    video: string,
    container: string,
    top: string,
    middle: string,
    bottom: string,
    icons: string,
    buttons: string,
}